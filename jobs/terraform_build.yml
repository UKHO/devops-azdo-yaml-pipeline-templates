parameters:
  - name: ArtifactName
    type: string
    default: 'TerraformArtifact'
    displayName: 'Artifact Name for Target Path to be saved as'

  - name: Pool
    type: string
    default: ''
    displayName: 'The pool to run this job on'

  - name: RelativePathToTerraformFiles
    type: string
    default: ''
    displayName: 'Target Path to Terraform files (.tf,.tfvars) that require publishing as artifact'

  - name: AdditionalFilesToPackage
    type: object
    default: [ ]
    displayName: 'List of additional files to include in the terraform artifact (SourceDirectory, FilesPattern, TargetSubdirectoryName)'

  - name: TerraformVersion
    type: string
    default: '1.14.0'
    displayName: 'Version of Terraform CLI tool to use with the terraform files'

  - name: TerraformBuildInjectionSteps
    type: stepList
    default: [ ]
    displayName: 'Steps to be carried out before the terraform is init, validated, and packaged'

jobs:
  - job: TerraformBuild_${{ parameters.ArtifactName }}
    ${{ if parameters.Pool }}:
      pool: ${{ parameters.Pool }}
    displayName: "Terraform Build '${{ parameters.ArtifactName }}'"
    variables:
      RepositoryCheckoutFolderName: $(Build.Repository.Name)
      RepositoryDirectory: $(Pipeline.Workspace)/$(RepositoryCheckoutFolderName)
      TerraformWorkingDirectory: $(RepositoryDirectory)/${{ parameters.RelativePathToTerraformFiles }}
    workspace:
      clean: all
    steps:
      - checkout: self
        displayName: 'Checkout self repository'
        clean: true
        path: $(RepositoryCheckoutFolderName)
        
      - ${{ each step in parameters.TerraformBuildInjectionSteps }}:
          - ${{ step }}

      - ${{ each additionalFileToPackage in parameters.AdditionalFilesToPackage }}:
          - template: /tasks/copy_files.yml
            parameters:
              SourceFolder: $(RepositoryDirectory)/${{ additionalFileToPackage.SourceDirectory }}
              FilesPattern: ${{ additionalFileToPackage.FilesPattern }}
              TargetFolder: $(TerraformWorkingDirectory)/${{ additionalFileToPackage.TargetSubdirectoryName }}

      - template: /tasks/terraform_installer.yml
        parameters:
          TerraformVersion: ${{ parameters.TerraformVersion }}

      - template: /tasks/terraform.yml
        parameters:
          Command: init
          CommandOptions: '-backend=false'
          WorkingDirectory: $(TerraformWorkingDirectory)

      - template: /tasks/terraform.yml
        parameters:
          Command: validate
          WorkingDirectory: $(TerraformWorkingDirectory)

      - template: /tasks/powershell.yml
        parameters:
          TaskName: 'RemoveTerraformDirectory'
          TargetType: 'inline'
          InlineScript: |
            Remove-Item -Path "$(TerraformWorkingDirectory)/.terraform" -Recurse -Force -ErrorAction SilentlyContinue
            Write-Host "Removed .terraform directory from artifact"
          FailOnStandardError: false

      - template: /tasks/publish_pipeline_artifact.yml
        parameters:
          TargetPath: $(TerraformWorkingDirectory)
          ArtifactName: ${{ parameters.ArtifactName }}
